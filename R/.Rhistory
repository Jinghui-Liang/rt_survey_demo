library(tidyverse)
con_t <- DBI::dbConnect(
RMariaDB::MariaDB(),
host = "test",
port = 3306,
user = 'root',
password = "example",
dbname = "test")
response <- tbl (con_t, "response") %>% collect()
frequency <- tbl (con_t, "frequency_counter") %>% collect ()
order <- tbl (con_t, "order_list") %>% collect ()
match <- tbl (con_t, "order_match") %>% collect ()
dbDisconnect(con_t)
con_t <- DBI::dbConnect(
RMariaDB::MariaDB(),
host = "127.0.0.1",
port = 3306,
user = 'root',
password = "example",
dbname = "test")
response <- tbl (con_t, "response") %>% collect()
frequency <- tbl (con_t, "frequency_counter") %>% collect ()
order <- tbl (con_t, "order_list") %>% collect ()
match <- tbl (con_t, "order_match") %>% collect ()
DBI::dbDisconnect(con_t)
View(order)
write.csv (order, "presentation_order_3.csv")
View(response)
con_t <- DBI::dbConnect(
RMariaDB::MariaDB(),
host = "127.0.0.1",
port = 3306,
user = 'root',
password = "example",
dbname = "test")
response <- tbl (con_t, "response") %>% collect()
View(response)
write.csv (order, "response_3.csv")
write.csv (response, "response_3.csv")
?tbl
?collect
?dbConnect
con_t <- DBI::dbConnect(
RMariaDB::MariaDB(),
host = "127.0.0.1",
port = 3306,
user = 'root',
password = "example",
dbname = "test")
order <- tbl (con_t, "order_list") %>% collect ()
library(tidyverse)
order <- tbl (con_t, "order_list") %>% collect ()
View(order)
DBI::dbDisconnect(con_t)
